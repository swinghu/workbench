CREATE KEYSPACE simplex WITH replication = {'class':'SimpleStrategy', 'replication_factor':3};

CREATE TABLE simplex.EmployeeInfo (
id int PRIMARY KEY,
age int,
fname text,
lname text,
salary bigint
);

INSERT INTO simplex.EmployeeInfo (id, age, fname, lname) 
VALUES (
1,
40,
'Doug',
'Cutting'
);